<Page x:Class="PrisonManagement.Views.Pages.ThamGapPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Quản lý Thăm gặp">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="👥 Quản lý Thăm gặp" FontSize="28" 
                   FontWeight="Bold" Foreground="#1e3a5f" Margin="0,0,0,20"/>

        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="10">
            <DataGrid x:Name="dgThamGap" AutoGenerateColumns="False" 
                      IsReadOnly="True" CanUserAddRows="False"
                      GridLinesVisibility="Horizontal" BorderThickness="0" RowHeight="45">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                    <DataGridTextColumn Header="Phạm nhân" Binding="{Binding PhamNhanTen}" Width="150"/>
                    <DataGridTextColumn Header="Người thăm" Binding="{Binding NguoiThamGap}" Width="150"/>
                    <DataGridTextColumn Header="Quan hệ" Binding="{Binding QuanHe}" Width="100"/>
                    <DataGridTextColumn Header="Ngày" Binding="{Binding NgayThamGap, StringFormat=dd/MM/yyyy}" Width="100"/>
                    <DataGridTextColumn Header="Thời gian" Binding="{Binding ThoiGian}" Width="100"/>
                    <DataGridTextColumn Header="Ghi chú" Binding="{Binding GhiChu}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>
