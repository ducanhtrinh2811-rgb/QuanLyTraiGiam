<Page x:Class="PrisonManagement.Views.Pages.LaoDongPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Quản lý Lao động">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="🔨 Quản lý Lao động" FontSize="28" 
                   FontWeight="Bold" Foreground="#1e3a5f" Margin="0,0,0,20"/>

        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="10">
            <DataGrid x:Name="dgLaoDong" AutoGenerateColumns="False" 
                      IsReadOnly="True" CanUserAddRows="False"
                      GridLinesVisibility="Horizontal" BorderThickness="0" RowHeight="45">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                    <DataGridTextColumn Header="Phạm nhân" Binding="{Binding PhamNhanTen}" Width="150"/>
                    <DataGridTextColumn Header="Công việc" Binding="{Binding CongViec}" Width="200"/>
                    <DataGridTextColumn Header="Ngày bắt đầu" Binding="{Binding NgayBatDau, StringFormat=dd/MM/yyyy}" Width="110"/>
                    <DataGridTextColumn Header="Ngày kết thúc" Binding="{Binding NgayKetThuc, StringFormat=dd/MM/yyyy}" Width="110"/>
                    <DataGridTextColumn Header="Trạng thái" Binding="{Binding TrangThai}" Width="100"/>
                    <DataGridTextColumn Header="Đánh giá" Binding="{Binding KetQuaDanhGia}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>
