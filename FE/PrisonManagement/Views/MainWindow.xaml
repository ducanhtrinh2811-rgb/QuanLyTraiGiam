<Window x:Class="PrisonManagement.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hệ thống Quản lý Trại giam" 
        Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized">
    
    <Window.Resources>
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2c5282"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="#1e3a5f">
            <DockPanel>
                <!-- Header -->
                <StackPanel DockPanel.Dock="Top" Margin="20">
                    <TextBlock Text="🏛️ Quản lý Trại giam" FontSize="18" 
                               FontWeight="Bold" Foreground="White"/>
                    <TextBlock x:Name="txtUserInfo" Foreground="#a0c4e8" 
                               FontSize="12" Margin="0,10,0,0"/>
                </StackPanel>

                <!-- Logout Button -->
                <Button DockPanel.Dock="Bottom" Content="🚪 Đăng xuất" 
                        Height="40" Margin="15" Background="#c0392b" 
                        Foreground="White" BorderThickness="0"
                        Click="BtnLogout_Click" Cursor="Hand"/>

                <!-- Menu -->
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        <Button Content="👤 Quản lý Phạm nhân" Tag="PhamNhan" 
                                Style="{StaticResource MenuButtonStyle}" 
                                Click="MenuButton_Click"/>
                        <Button Content="👮 Quản lý Cán bộ" Tag="CanBo" 
                                Style="{StaticResource MenuButtonStyle}" 
                                Click="MenuButton_Click"/>
                        <Button Content="🚪 Quản lý Phòng giam" Tag="PhongGiam" 
                                Style="{StaticResource MenuButtonStyle}" 
                                Click="MenuButton_Click"/>
                        <Button Content="🏥 Quản lý Sức khỏe" Tag="SucKhoe" 
                                Style="{StaticResource MenuButtonStyle}" 
                                Click="MenuButton_Click"/>
                        <Button Content="👥 Quản lý Thăm gặp" Tag="ThamGap" 
                                Style="{StaticResource MenuButtonStyle}" 
                                Click="MenuButton_Click"/>
                        <Button Content="🔨 Quản lý Lao động" Tag="LaoDong" 
                                Style="{StaticResource MenuButtonStyle}" 
                                Click="MenuButton_Click"/>
                        <Button Content="🏆 Quản lý Khen thưởng" Tag="KhenThuong" 
                                Style="{StaticResource MenuButtonStyle}" 
                                Click="MenuButton_Click"/>
                        <Button Content="⚖️ Quản lý Kỷ luật" Tag="KyLuat" 
                                Style="{StaticResource MenuButtonStyle}" 
                                Click="MenuButton_Click"/>
                        <Button Content="🚨 Quản lý Sự cố" Tag="SuCo" 
                                Style="{StaticResource MenuButtonStyle}" 
                                Click="MenuButton_Click"/>
                    </StackPanel>
                </ScrollViewer>
            </DockPanel>
        </Border>

        <!-- Main Content -->
        <Border Grid.Column="1" Background="#f5f5f5">
            <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden"/>
        </Border>
    </Grid>
</Window>