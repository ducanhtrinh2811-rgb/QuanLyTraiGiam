<Page x:Class="PrisonManagement.Views.Pages.SucKhoePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Quản lý Sức khỏe">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="🏥 Quản lý Sức khỏe" FontSize="28" 
                   FontWeight="Bold" Foreground="#1e3a5f" Margin="0,0,0,20"/>

        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="10">
            <DataGrid x:Name="dgSucKhoe" AutoGenerateColumns="False" 
                      IsReadOnly="True" CanUserAddRows="False"
                      GridLinesVisibility="Horizontal" BorderThickness="0" RowHeight="45">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                    <DataGridTextColumn Header="Phạm nhân" Binding="{Binding PhamNhanTen}" Width="150"/>
                    <DataGridTextColumn Header="Ngày khám" Binding="{Binding NgayKham, StringFormat=dd/MM/yyyy}" Width="100"/>
                    <DataGridTextColumn Header="Tình trạng" Binding="{Binding TinhTrangSucKhoe}" Width="120"/>
                    <DataGridTextColumn Header="Chẩn đoán" Binding="{Binding ChanDoan}" Width="200"/>
                    <DataGridTextColumn Header="Điều trị" Binding="{Binding DieuTri}" Width="200"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>
