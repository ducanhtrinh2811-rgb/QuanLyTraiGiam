<Page x:Class="PrisonManagement.Views.Pages.CanBoPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Quản lý Cán bộ">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="👮 Quản lý Cán bộ" FontSize="28" FontWeight="Bold" 
                       Foreground="#1e3a5f" VerticalAlignment="Center"/>
            <Button Content="➕ Thêm mới" Margin="30,0,0,0"
                    Padding="20,10" Background="#3182ce" Foreground="White"
                    BorderThickness="0" FontSize="14" Cursor="Hand"/>
            <Button Content="🔄 Làm mới" Margin="10,0,0,0"
                    Padding="20,10" Background="#38a169" Foreground="White"
                    BorderThickness="0" FontSize="14" Cursor="Hand"
                    Click="BtnRefresh_Click"/>
        </StackPanel>

        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="10">
            <DataGrid x:Name="dgCanBo" AutoGenerateColumns="False" 
                      IsReadOnly="True" CanUserAddRows="False"
                      GridLinesVisibility="Horizontal" BorderThickness="0" RowHeight="45">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                    <DataGridTextColumn Header="Họ tên" Binding="{Binding HoTen}" Width="150"/>
                    <DataGridTextColumn Header="Chức vụ" Binding="{Binding ChucVu}" Width="150"/>
                    <DataGridTextColumn Header="Phòng ban" Binding="{Binding PhongBan}" Width="150"/>
                    <DataGridTextColumn Header="SĐT" Binding="{Binding SoDienThoai}" Width="120"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="180"/>
                    <DataGridTextColumn Header="Trạng thái" Binding="{Binding TrangThai}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>
