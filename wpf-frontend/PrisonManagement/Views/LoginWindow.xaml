<Window x:Class="PrisonManagement.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Đăng nhập - Hệ thống Quản lý Trại giam" 
        Height="450" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1e3a5f">
    
    <Grid>
        <Border Background="White" CornerRadius="10" Margin="30" Padding="30">
            <StackPanel VerticalAlignment="Center">
                <!-- Logo/Title -->
                <TextBlock Text="🏛️" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="Hệ thống Quản lý" FontSize="24" FontWeight="Bold" 
                           HorizontalAlignment="Center" Foreground="#1e3a5f"/>
                <TextBlock Text="Trại giam" FontSize="20" FontWeight="Bold" 
                           HorizontalAlignment="Center" Foreground="#1e3a5f" Margin="0,0,0,30"/>
                
                <!-- Username -->
                <TextBlock Text="Tên đăng nhập" Margin="0,0,0,5" Foreground="#666"/>
                <TextBox x:Name="txtUsername" Height="40" Padding="10,8" FontSize="14"
                         BorderBrush="#ddd" BorderThickness="1">
                    <TextBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>
                
                <!-- Password -->
                <TextBlock Text="Mật khẩu" Margin="0,15,0,5" Foreground="#666"/>
                <PasswordBox x:Name="txtPassword" Height="40" Padding="10,8" FontSize="14"
                             BorderBrush="#ddd" BorderThickness="1">
                    <PasswordBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </PasswordBox.Resources>
                </PasswordBox>
                
                <!-- Error Message -->
                <TextBlock x:Name="txtError" Foreground="Red" FontSize="12" 
                           Margin="0,10,0,0" TextWrapping="Wrap"/>
                
                <!-- Login Button -->
                <Button x:Name="btnLogin" Content="Đăng nhập" Height="45" 
                        Margin="0,20,0,0" FontSize="16" FontWeight="Bold"
                        Background="#1e3a5f" Foreground="White" 
                        BorderThickness="0" Cursor="Hand"
                        Click="BtnLogin_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
                
                <!-- Demo credentials -->
                <TextBlock Text="Demo: admin / admin123" FontSize="11" 
                           Foreground="#999" HorizontalAlignment="Center" 
                           Margin="0,15,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
