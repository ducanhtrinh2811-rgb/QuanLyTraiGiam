<Page x:Class="PrisonManagement.Views.Pages.KhenThuongPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Quản lý Khen thưởng">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="🏆 Quản lý Khen thưởng" FontSize="28" 
                   FontWeight="Bold" Foreground="#1e3a5f" Margin="0,0,0,20"/>

        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="10">
            <DataGrid x:Name="dgKhenThuong" AutoGenerateColumns="False" 
                      IsReadOnly="True" CanUserAddRows="False"
                      GridLinesVisibility="Horizontal" BorderThickness="0" RowHeight="45">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                    <DataGridTextColumn Header="Phạm nhân" Binding="{Binding PhamNhanTen}" Width="150"/>
                    <DataGridTextColumn Header="Ngày" Binding="{Binding NgayKhenThuong, StringFormat=dd/MM/yyyy}" Width="100"/>
                    <DataGridTextColumn Header="Loại" Binding="{Binding LoaiKhenThuong}" Width="150"/>
                    <DataGridTextColumn Header="Lý do" Binding="{Binding LyDo}" Width="250"/>
                    <DataGridTextColumn Header="Số ngày giảm" Binding="{Binding SoNgayGiam}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>
